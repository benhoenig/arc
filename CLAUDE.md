# CLAUDE.md

> Read this first every session. It's a map to deeper docs — not a textbook.

## Project in one paragraph

Property flipping management system for a Thai small-team operation. Internal tool first, commercial product later. Replaces Google Sheets chaos across 4 teams (Sourcing, PM, Contractor Manager, Sales) running 10+ simultaneous flips. **Central entity:** `Flip` (investment lifecycle on a `Property`). Everything hangs off the Flip.

**Stack:** Next.js 16 + TypeScript + **Neon (Postgres)** + **Neon Auth** (managed Better Auth) + Prisma 7 + **Vercel Blob** (file storage) + Tailwind v4 + shadcn/ui + IBM Plex Sans Thai + next-intl 4. App package name is `arc`; GitHub repo is `benhoenig/arc`. _(Migrated off Supabase — DB/Auth/Storage — in 2026-06; see `db/README.md` and memory `project_neon_migration.md`. Historical Supabase SQL is archived in `supabase/migrations/`.)_

## Where things are documented

Read the relevant doc before generating code. Don't guess at patterns.

| Need to... | Read |
|---|---|
| Understand scope or vision | `docs/PRODUCT_SPEC.md` |
| Write SQL / migration / query | `docs/DATA_MODEL.md` |
| Style or build UI | `docs/DESIGN_SYSTEM.md` |
| Write any code | `docs/CONVENTIONS.md` |
| Know which tool/version | `docs/TECH_STACK.md` |
| Know what to build next | `docs/IMPLEMENTATION_PLAN.md` |

## Current build state

> **Ben: update this after each milestone.**

- **Infra migration (2026-06):** ARC moved off Supabase → **Neon** (Postgres + Neon Auth) + **Vercel Blob** (storage). All app code, schema, auth, and storage are ported and validated end-to-end. Follow-ups: rotate the Neon DB password before real data; the old `supabase/migrations/` is historical only (baseline is `db/migrations/0000_baseline_neon.sql`). Details in memory `project_neon_migration.md`.
- **Current milestone:** M8 — Investors & Capital (next up). **M7 Tasks & Timeline shipped.** **M6.7 AI Document Extraction (OCR) shipped out-of-sequence** at Ben's request (built right after M6, before M7). M8 when reached has a scope gap flagged in memory `project_investor_loan_pool_model.md` (loan-pool model for flat_interest investors) that must be added to IMPLEMENTATION_PLAN.md §11 before writing migrations.
- **Last completed:** M0/M1/M2 + **M3 Flips Core** + **M3.5 Invitations & Members** + **M3.6 Pivot / Re-underwriting + Revive + Remove Member** + **M4 Budget** + **M4.5 Flip transactions & receipts** + **M4.6 Live P&L / feasibility panel** + **M5 Contractors directory & assignments** + **M6 Contractor Payments** + **M6.7 AI Document Extraction (OCR)** + **M7 Tasks & Timeline** — all shipped. M7 delivered `tasks` + `milestones` (flip-level, distinct from contractor milestones) tables + `flip_portfolio_dashboard` view extension (`open_tasks_count` + `overdue_tasks_count`) in migration `db/migrations/0006_tasks_milestones.sql` (**NOT yet applied to production — Ben must apply it**); feature module `src/features/tasks` (validators/queries/actions/components); per-flip task list at `/flips/[id]/tasks` (`TaskListPanel` with inline `TaskQuickAdd` + detailed `TaskForm` dialog + per-row complete/reopen/edit/delete), flip-level milestone timeline at `/flips/[id]/timeline` (`TimelinePanel` with target-vs-actual + on-time/early/late variance pill), cross-flip personal inbox at `/my-tasks` (`MyTasksList`, grouped by flip) wired to a new **My Tasks** sidebar item (standalone, next to Dashboard), read-only `FlipTasksPanel` on flip detail (open/overdue counts + next milestone), `DueDatePill` with a pure exported `dueDateBucket()` helper (overdue=destructive+bold, today/tomorrow=warning, future=neutral, done/none=muted), new `tasks` i18n namespace. **Completion stamps are action-controlled** — `completeTask` is the only path to `status='done'` (sets `completed_at`/`completed_by`); `reopenTask` clears them; plain `updateTask` can't set `done` (mirrors the M6 "status set by money flow" precedent). Unlike budget lines, tasks are NOT locked on sold/killed flips (post-sale follow-ups are legit). M6.7 (out-of-sequence, Ben-requested) added `src/features/ocr`: upload an image/PDF → pick a target (transaction / budget line / contractor milestone / T&M entry / contractor) + single-vs-batch → Claude (default `claude-sonnet-4-6`, override via `OCR_MODEL`) extracts structured rows via **structured outputs** (Zod `zodOutputFormat` + `messages.parse`) → operator reviews/edits an editable table → bulk-save in one `db.$transaction`. Auth supports **either** `ANTHROPIC_API_KEY` **or** a Claude **subscription** OAuth token (`ANTHROPIC_AUTH_TOKEN`, sent as Bearer + `anthropic-beta: oauth-2025-04-20`); the client prefers the API key, falls back to the token, and is null (extraction disabled) when neither is set. `<ExtractDialog>` wired into the flip budget page (transaction + budget_line), assignment detail (milestone OR tm_entry by payment model), and contractors directory (contractor). New `ocr` i18n namespace. **No DB migration** — pure composition over existing tables; batch transactions reuse the M4.5 `kind='spend'` path (each row requires a per-row budget-line pick in the review table). M6 delivered `contractor_milestones` + `contractor_tm_entries` + `contractor_payments` tables (migration `db/migrations/0002_contractor_payments.sql`, **validated on a Neon branch — NOT yet applied to production; Ben must apply it**), milestone + T&M status machines (`canTransitionMilestoneStatus` / `canTransitionTmEntryStatus` mirroring M5's assignment helper), `computeTmLineTotal` (labor = rate×days/hours, material = cost×(1+markup%)), full action set (milestone + T&M CRUD/status, request/approve/reject/cancel payment, `markPaymentPaid`), `/contractors/payments` queue + `/flips/[id]/contractors/[assignmentId]` detail route, `payments` i18n namespace, Payments sidebar item. **The keystone** — `markPaymentPaid` emits a `flip_transactions` `kind='spend'` row (app-code, NOT a DB trigger) so the existing `recompute_budget_line_actual` trigger updates the budget actual; it **auto-creates the assignment's budget line when none exists** (decision: option B — category = assignment's `budget_category_id`, else org `contingency` fallback) and settles the source (milestone → `paid`, or the T&M batch's entries via `metadata.tmEntryIds` → `paid`). Two DB triggers added: `recompute_contractor_paid` (`total_paid_thb` on contractor + assignment) and `recompute_assignment_committed` (`total_committed_thb` from milestones + approved/paid T&M, attached to both source tables). `flip_transactions.contractor_payment_id` link column (unique among non-deleted) guards against double-emitting a spend. M5 delivered `contractors` + `contractor_assignments` tables with the full `payment_model` discriminator (`fixed_milestone | time_materials | progress_payment`), `contractor_active_commitments` view (Q7 conflict detection), the previously-deferred FK on `budget_lines.contractor_assignment_id`, assignment-status state machine enforced in a `canTransitionAssignmentStatus()` helper used by both the validator and UI (draft → active → completed/canceled/disputed; completed/canceled are terminal), `/contractors` directory + `/contractors/[id]` detail + `/flips/[id]/contractors` sub-route, conflict-warning UI that queries existing overlapping assignments in real time from the `useEffect` in `CreateAssignmentDialog` and asks the user to explicitly override before submitting. No payment tracking yet — that's M6. M4.6 delivered `getFlipPnl(orgId, flipId)` composing latest revision OR baseline + deal-analysis overheads (holding/transaction/selling/marketing/other) + `flip_budget_summary` + `flip_cash_summary` into a single projection with planned/projected/realized profit + margin + ROI. Read-only `<FlipFeasibilityPanel>` renders a 3-column (plan/actual/variance) cost table with directionality-aware variance coloring (overrun = warning/destructive, under budget = positive) and a 3-card profit/margin/ROI summary. Untracked categories (everything except purchase + renovation) fall through to plan for the projection and display `—` in the actual column with a hover hint. No new tables — pure composition. M4.5 delivered `flip_transactions` signed-amount ledger (inflows + outflows, 6 kinds: investor_deposit/loan_disbursement/spend/refund/sale_proceeds/distribution, 3 CHECKs enforcing sign + budget_line vs kind + enum), `flip_cash_summary` view, `recompute_budget_line_actual()` trigger that maintains `budget_lines.actual_amount_thb = SUM(non-deleted transactions)` handling OLD+NEW on UPDATE, private `budget-receipts` Supabase Storage bucket with org-scoped path RLS (`{orgId}/{flipId}/{uuid}.{ext}`) and 5-min signed-URL reads via server action, removal of inline `actualAmountThb` editing from budget table (now read-only, computed from the view), `AddTransactionDialog` with kind-aware discriminated-union validation (spend/refund require budget_line, investor_deposit/loan_disbursement require source_note), `FlipTransactionList` with per-row soft-delete + receipt thumbnail, `FlipCashBalanceIndicator` pill on flip detail header alongside budget variance pill, M4 smoke-test `actual_amount_thb` values zeroed as part of the migration (fresh ledger from day one). M3 delivered the full deal-to-flip workflow: `flips` + `flip_team_members` + `flip_code_counters` with RLS, atomic `FLIP-YYYY-###` code generation, createFlip-from-deal action, detail page (header + overview panel + team panel), stage transitions with terminal-stage guards, kill flow, `/flips` + `/flips/[id]` + `/flips/[id]/team` routes, "Convert to Flip" button on deal-analysis cards. M3.5 added `org_invitations` with SHA-256-hashed email-bound tokens, admin-only invite/revoke/remove-member actions, `/settings/members` page (tabs: members + pending invites), public `/invite/[token]` accept page, Members nav item, two SECURITY DEFINER DB functions bridging the RLS gap. M3.6 added `flip_revisions` with three compute modes (float_reunderwrite spread math, transfer_reunderwrite full-ARV math, pivot_to_transfer_in), `flips.flip_type` column that flips on pivot, contract-extension line items (refundable additional deposit + non-refundable additional expense), revive action for undoing kills, remove-member action with safety guards, `contract_expired` kill reason, target-profit row on the overview panel (colored by sign). **M4** delivered three-state budget tracking: `budget_categories` (15 seeded defaults per org) + `budget_lines` (budgeted/committed/actual) with RLS, `flip_budget_summary` + `category_budget_summary` views, extended `flip_portfolio_dashboard` with budget aggregates, feature module (`/src/features/budget`) with validators/queries/actions/UI, inline-editable budget table with thousand-separator inputs, variance card + burn bar + category breakdown, clickable variance pill on flip detail header linking to `/flips/[id]/budget`, inline summary panel on flip detail (read-only — full editing on sub-route), admin-only `/settings/budget-categories` for managing org categories.
- **M1 deferred items** still outstanding (not needed for M4): forgot-password pages, locale switcher UI, `users.locale` persistence, dark mode toggle, unit tests for formatters, Playwright E2E tests.
- **M2 deferred items**: full property photo gallery (single thumbnail shipped; gallery stays in M11), unit + integration tests for deal-analysis compute (defer until vitest/playwright is wired).
- **M3 deferred items**: edit-flip UI (action exists but no form — overview panel still shows "—" for actuals until built); expanded `flip_portfolio_dashboard` view aggregates (budget in M4, tasks in M7); integration + Playwright tests for the flip flow.
- **M3.5 deferred items**: existing-user-joins-second-org is blocked (see memory `project_multi_org_gap.md`); no org-switcher UI; no transactional email — admins share links manually.
- **M3.6 deferred items** (see memory `project_post_m4_autofill.md`): sunk-cost fields in revision dialogs are manual-entry until M4 actuals tracking exists — pre-filling from budgets would mislead operators into thinking "spent exactly what was planned." Post-M4 follow-up: wire sunk fields to `SUM(budget_lines.actual_amount_thb)` per category (updated post-M4.5: source will be `flip_transactions` rollup once that ships).
- **M4 deferred items**: transaction logging + receipts (✅ shipped in M4.5); live P&L / feasibility panel (addressed by M4.6); role-permission gates on budget actions (everything is org-member open today — same pattern as other modules, tracked in `project_role_permissions_gap.md`); `/settings/budget-categories` is reachable only by URL (no sidebar link — deliberate, there's no settings landing page yet); integration + Playwright tests (same defer pattern as prior milestones).
- **M4.5 deferred items**: investor FK on inflow transactions (ship blocker for **M8** — `source_note` is free text until then, see memory `project_m4_5_transactions_and_m8_investor_link.md`. Previously mis-tracked to M10 — that memory has been corrected and the old file deleted); no inline edit of existing transactions (only create + soft-delete — correct a bad row by deleting and re-creating); contractor-payment kind unification (M6 will route contractor payments through `flip_transactions` as `kind='spend'` tagged to the assignment's budget line); integration + Playwright tests.
- **M4.6 deferred items**: per-category actual tracking for holding / transaction / selling / marketing / other costs — today those fall back to their planned values in the projection. Wiring them through would require either extending budget categories to cover overheads, or adding kind-to-category mapping on `flip_transactions` for non-line-bound spend. Tracked for post-M5 when contractor payments land and the ledger has more real data to test against; sunk-cost auto-fill in revision dialogs (now sourced from `flip_transactions` rollup — unblocked by M4.5, still deferred); integration + Playwright tests.
- **M5 deferred items**: edit-assignment UI (action exists, no form — status changes work via the inline status select, but changing dates/amounts/budget_category requires a future form); contractor form edit mode (create works, edit doesn't — operators can update via direct DB today if needed); contractor performance rollups (schema has `avg_on_time_pct`, `avg_quality_rating`, `last_assignment_at`, `total_assignments_count`, `total_paid_thb` — not populated yet. Will be trigger-maintained when M6 lands with milestones + payments giving on-time data and M11 when reviews land for quality); multiselect for `additional_trades` in contractor form (create dialog only sets primary trade today — additional_trades array stays empty; trivial to add as a follow-up if operators need it); `/flips/[id]/contractors` is reachable only via the panel link on flip detail, no deep-link breadcrumb; role permissions (same gap as prior modules — anyone in the org can create/edit/delete); integration + Playwright tests.
- **Cross-cutting gaps tracked in memory:**
  - **Role permissions not enforced** beyond `admin` (invite/revoke/remove). Every other server action is open to any org member. See `project_role_permissions_gap.md`. Plan: wire per-feature as milestones land; full pass after M7.
- **Key architectural decisions made during M2:**
  - **shadcn token aliases in `globals.css`** — shadcn components reference `--color-popover`, `--color-accent`, `--color-muted-foreground` etc. which are mapped to ARC tokens. New shadcn primitives should consume ARC tokens directly (e.g. `bg-background`, `text-text-default`) rather than shadcn class names to avoid resolution ambiguity.
  - **Decimal serialization** — Prisma returns `Decimal` objects that can't cross the server/client boundary. Every query that feeds a Client Component must `Number()`-convert its Decimal columns (see `get-property.ts`, `get-flip.ts`, `list-flips.ts` for the pattern). Bites every time a new Decimal column is selected.
  - **`useTransition` + `revalidatePath` hang** — the `DealAnalysisForm` uses plain `useState` instead of `useTransition` because `useWatch` + `form.reset()` + `revalidatePath` inside a transition caused `isPending` to never settle. Only this form is affected; the pattern with `useTransition` is fine elsewhere.
  - **Storage bucket pattern** — `property-thumbnails` is public with UUID-based paths (`{orgId}/{uuid}.{ext}`). Adequate for internal tool; revisit with signed URLs pre-commercial.
- **Key architectural decisions made during M3 / M3.5 / M3.6:**
  - **Sequential entity codes via a counter table** — `flip_code_counters` (org_id, year, next_number) + single-statement `INSERT ... ON CONFLICT DO UPDATE ... RETURNING (next_number - 1)` is the concurrency-safe way to allocate per-org human codes. Reuse the pattern for future entities (POs, budget refs).
  - **`moveFlipToStage` blocks moves to `killed`** — users must call `killFlip` so reason capture is enforced. Moves from terminal stages (sold/killed) are blocked entirely. The stage `<Select>` hides `killed` from its options.
  - **Dev-server Prisma client caching** — `globalThis.db` caches the generated client in dev. After `prisma generate` you must fully restart `pnpm dev`; HMR won't re-import the client, and runtime calls (`db.flip.findMany`) will blow up with "cannot read properties of undefined".
  - **Invitation tokens: hashed at rest, email-locked.** SHA-256 in DB, raw only in URL. `chk_invitation_email_lower` CHECK keeps emails canonical. Unique partial index on `(org_id, email) WHERE pending` prevents duplicate outstanding invites.
  - **SECURITY DEFINER for anon/new-user flows.** `get_invitation_by_hash` and `accept_invitation` are the only paths that bypass RLS — the accepter has no `user_roles` row until the invite is consumed. `accept_invitation` is called via `supabase.rpc()` (not Prisma) so `auth.uid()` resolves from the JWT session cookie; a Prisma pooled connection wouldn't carry the claim.
  - **Admin check lives in app code, not RLS.** `isOrgAdmin()` at `src/server/shared/require-admin.ts`. RLS still enforces org-membership only; finer-grained gates are per-action. Keeps RLS policies simple and debuggable.
  - **Profit math branches by flip_type, not by revision_type alone.** `computeFlipRevisionTotals(mode, input)` has three modes: `float_reunderwrite` (spread math: new sale − SPA contract − costs), `transfer_reunderwrite` and `pivot_to_transfer_in` (both full-ARV math). The same `revision_type = 'reunderwrite'` record produces different math depending on the flip's current type at commit time. Pivot ALSO writes `flip_type = 'transfer_in'` on the flip, so any subsequent re-underwrite uses transfer math.
  - **Refundable vs non-refundable capital.** In float re-underwrite, `new_additional_deposit` counts in total capital deployed (affects ROI) but NOT in cost subtraction (refundable on success). `new_additional_expense` counts in both. This asymmetry is intentional — ROI needs capital-at-risk, profit needs actual cost.
  - **Revisions are append-only history.** No UPDATE policy on `flip_revisions`; correct a bad revision by writing a new one on top. The flip's `baseline_*` columns always reflect the latest revision's numbers.
  - **Sunk-cost inputs are manual until M4 budget actuals exist.** Pre-filling from budget values would mislead (assumes perfect adherence). See `memory/project_post_m4_autofill.md` for the follow-up plan (now deferred past M4.5 so source is `flip_transactions` rollup, not direct `budget_lines.actual_amount_thb`).
- **Key architectural decisions made during M4:**
  - **Partial unique indexes, not inline table UNIQUE clauses.** `UNIQUE (organization_id, slug) WHERE deleted_at IS NULL` is only valid as `CREATE UNIQUE INDEX`, not as an inline table constraint. Use the index form for any "unique among non-deleted rows" pattern.
  - **Views for rollups, `$queryRaw` to read them.** `flip_budget_summary` + `category_budget_summary` aren't in Prisma — accessed via `db.$queryRaw` in `get-flip-budget-summary.ts` / `get-category-breakdown.ts`. Pattern: keep aggregation logic in SQL, raw-query in TS, Number-convert the numerics on the way out.
  - **Deferred FK columns.** `budget_lines.contractor_assignment_id` exists with no FK constraint — M5 adds the constraint when `contractor_assignments` lands. Avoids a circular migration dependency.
  - **Thousand-separator inputs use `type="text"` + `inputMode="decimal"`.** Browsers don't format `type="number"` with commas. See `src/features/budget/components/amount-input-helpers.ts` for `cleanNumericInput` / `formatWithCommas` / `parseAmount`. Reuse for any currency input that needs comma display while editing.
  - **Clickable pill pattern.** `BudgetVariancePill` accepts an optional `href` and wraps in `<Link>` when provided. Summarizes + navigates in one element — preferable to pairing a pill with a redundant "View budget" button. Reuse for other summary-as-CTA slots.
  - **Inline summary + sub-route editing.** `FlipBudgetPanel` on the flip detail page renders read-only summary; `/flips/[id]/budget` is the single source of truth for line-level edits. Avoids two UIs mutating the same data. Apply the same pattern to future heavy sub-features (tasks, payments).
  - **Turbopack `.next/dev/` cache outlasts `pnpm dev` restarts.** Regenerating the Prisma client under `src/generated/prisma/` requires `rm -rf .next` before restarting, or Turbopack serves stale compiled chunks that reference the old client. Killing the dev process is not enough. **If `rm -rf` is permission-denied in the harness, use `mv .next .next.stale` BUT delete the stale dir in the same session** — accumulated `.next.stale-*` dirs hit 1.5GB in one session and contributed to macOS Spotlight + filesystem pressure. See memory `project_dev_perf_footguns.md`.
  - **Prisma's generated client lives OUTSIDE `src/`** at `prisma/generated/client/`, imported via the `@prisma-client/*` tsconfig path alias. Tsconfig exclude alone did NOT fix the 2000% CPU hang — Turbopack watches by import graph, not by tsconfig. Moving the output outside the `src/` watch scope is the actual fix. `.next/` cache bloat hit 11GB when the generated client was under `src/`. When adding any framework that generates code into the repo, never co-locate generated output inside `src/`.
  - **Never run `next build` while `pnpm dev` is also running.** The build overwrites `.next/` and corrupts the dev server, causing ChunkLoadError on the next page load. Stop dev first, build, then restart dev.
- **Key architectural decisions made during M4.5:**
  - **Signed amounts, not separate in/out tables.** `flip_transactions.amount_thb` is signed (positive = inflow, negative = outflow) with a DB CHECK that enforces the convention by kind. One table = one rollup, and sale proceeds / distributions slot into the same enum without a schema change when M9/M10 land. API accepts positive magnitude from UI; `toSignedAmount(kind, magnitude)` flips sign server-side.
  - **Zod `z.input` (not `z.infer`) for server-action payload types.** Server Actions serialize args — Date instances don't survive the RSC boundary. When a schema contains `.transform()` that coerces strings to Dates, expose the pre-transform input type so clients can send strings. `createFlipTransactionSchema` + `updateFlipTransactionSchema` use this pattern.
  - **Trigger-maintained rollup, `actualAmountThb` removed from write validators.** `recompute_budget_line_actual()` is the single source of truth. To lock out the back door, `actualAmountThb` is stripped from `createBudgetLineSchema`/`updateBudgetLineSchema` — the Zod schema enforces the invariant before Prisma sees it.
  - **`'use server'` files can only export async functions.** Constants (like bucket names) must live in a separate module. `RECEIPT_BUCKET` lives in `src/features/budget/lib/receipt-bucket.ts` — importing a const from a server-action file fails at build time with "Export doesn't exist in target module".
  - **Private bucket + signed-URL server action.** `budget-receipts` is private (receipts contain bank details). Reads go through `getReceiptSignedUrl` which double-checks the path prefix matches the caller's active org before minting a 5-min signed URL. Upload is still direct from the browser — RLS on `storage.objects` enforces path ownership on write.
  - **Docs drift from live DB caught here.** DATA_MODEL.md §14.1 says `set_updated_at` with trigger name `trg_<table>_updated_at`; the live DB uses `update_updated_at` with trigger name `set_<table>_updated_at`. Same drift for RLS policy names (`_write` in docs vs `_insert` in DB). When writing migrations, verify the exact function/trigger/policy name against the DB via MCP `execute_sql` rather than trusting the doc. DATA_MODEL.md §14.1 + §15.2 need a follow-up sync.
- **Key architectural decisions made during M5:**
  - **One `setAssignmentStatus` action, not three.** The plan listed `activateAssignment` / `completeAssignment` / `cancelAssignment` as separate actions. Collapsed into one action + a `canTransitionAssignmentStatus(from, to)` helper that's the single source of truth for legal transitions. UI calls the same action with different `status` args; adding a new transition is a one-line edit of the map. If this pattern repeats elsewhere (flip stages already does something similar), reuse.
  - **DB CHECK on payment-model fields is belt-and-suspenders with the Zod discriminated union.** `chk_payment_model_fields` at the DB level catches any path that bypasses validation (raw inserts, future API calls). The Zod union catches it before the request leaves the client. Both are cheap and catch different bug classes.
  - **Conflict detection is a query, not a constraint.** `getContractorConflicts(contractorId, startDate, endDate)` runs on every date/contractor change in the create dialog via `useEffect`. Shows overlapping assignments as a warning + requires explicit "proceed anyway" checkbox before submit. Matches Q7: contractors can legitimately work multiple flips in parallel, so the system warns but never blocks.
  - **`deleteContractor` blocks when active/draft assignments exist.** Prevents orphaning live work. Operator must cancel/complete assignments first. Same pattern should apply to other entities with outbound FKs (e.g. `deleteBudgetCategory` already blocks).
  - **Deferred FK wiring happens in the milestone that creates the target table.** `budget_lines.contractor_assignment_id` existed as an unconstrained column from M4. M5's migration adds the FK constraint. Pattern: when deferring an FK across milestones, leave the column in place and add `ADD CONSTRAINT FOREIGN KEY` in the later migration rather than dropping and re-adding the column.
- **Key architectural decisions made during M6:**
  - **Budget sync is app-code, contractor rollups are DB triggers.** `markPaymentPaid` inserts the `flip_transactions` spend row itself (inside the action's `db.$transaction`) rather than via a DB trigger — the ledger row needs `created_by` + a human `description` that a trigger can't supply cleanly. This is a deliberate deviation from `DATA_MODEL.md` §14.4's "trigger" wording (noted in the migration header). The simpler `total_paid_thb` / `total_committed_thb` rollups ARE DB triggers (recompute-style SUM, idempotent under soft-delete) per the M4.5 §14.5 precedent. Rule of thumb: rollup of a single numeric column → DB trigger; cross-table insert that needs request context → app-code in the action.
  - **Payment→budget-line auto-create (decision: option B).** A `kind='spend'` transaction REQUIRES a `budget_line_id` (DB CHECK), but M5 made the assignment→budget-line link optional. So `markPaymentPaid` auto-creates the line when absent: category = the assignment's `budget_category_id`, else the org's `contingency` category, else the first category. The line is tagged with `contractor_assignment_id` so the next payment reuses it.
  - **No payment↔T&M-entry FK; the batch is recorded in `metadata`.** The schema has `contractor_payments.milestone_id` but no column for a T&M batch. A `tm_batch` payment stores the covered entry ids in `metadata.tmEntryIds`; `markPaymentPaid` flips exactly those (still-approved) entries to `paid`. Keeps the schema as-specced without a join table.
  - **`flip_transactions.contractor_payment_id` (unique among non-deleted) is the double-emit guard.** Combined with the `status='approved' → 'paid'` precondition, a payment can mint at most one ledger spend row.
  - **Status set by the money flow, not the status menu.** Milestone `paid` and T&M-entry `paid` are reachable ONLY through `markPaymentPaid` — the manual `setMilestoneStatus`/`setTmEntryStatus` helpers reject `paid` (`conflict:paid_set_by_payment`). Mirrors how `moveFlipToStage` refuses `killed`.
- **M6 deferred items:** payments on a sold/killed flip are blocked (stays consistent with the M4.5 ledger invariant — paying a final contractor after sale needs revive today; tracked as a follow-up); no edit-milestone amount once `paid`, no edit of approved/paid T&M entries (reopen to pending to edit); notifications on milestone-completed deferred to M11 (no `notifications` table yet); role permissions still org-member-open (same gap as prior modules — approve/mark-paid are natural admin-only candidates for the post-M7 pass); `contractor_reviews` table exists in the baseline but performance rollups (`avg_on_time_pct`, `avg_quality_rating`) still unpopulated — wire when M11 reviews land; integration + Playwright tests (same defer pattern).
- **Next up:** **M8 — Investors & Capital Commitments.** Investor directory + commitments per flip. ~16–24h. See `IMPLEMENTATION_PLAN.md` §11 — but read the **M8 pre-work blocker** below FIRST (loan-pool model must be added to the plan + data model before writing migrations).
- **M8 pre-work blocker:** before writing M8 migrations, update `IMPLEMENTATION_PLAN.md` §11 and `DATA_MODEL.md` §8 to add the `investor_loans` + `loan_deployments` tables. See memories `project_m4_5_transactions_and_m8_investor_link.md` and `project_investor_loan_pool_model.md`. Loan pool (flat_interest) and equity per-deal (equity_split/preferred_return) are genuinely different instruments and must live in separate tables.
- **Ben's pending actions:** (1) rotate DB password, (2) upgrade Supabase to Pro before real data, (3) manual smoke test M5: create contractor → assign to FLIP → try overlapping assignment on same contractor → see conflict warning → override → verify on `/contractors/[id]` that assignment history shows both flips, (4) **apply M6 migration `db/migrations/0002_contractor_payments.sql` to the Neon `production` branch** (validated on a throwaway branch, not yet on prod) then restart dev with a clean `.next`, (5) manual smoke test M6 (demo script in `IMPLEMENTATION_PLAN.md` §9.3): fixed-milestone assignment → add milestones → complete → request → approve → mark paid → verify the budget line's actual jumps + cash pill drops; T&M assignment → log labor + material → approve → batch-request → pay, (6) **configure OCR (M6.7):** set EITHER `ANTHROPIC_API_KEY` OR `ANTHROPIC_AUTH_TOKEN` (Claude subscription token from `ant auth login` / Claude Code) in the environment, and ensure the remote env's **network policy allows outbound to `api.anthropic.com`** (otherwise extraction calls are blocked). Optionally set `OCR_MODEL` to override the default `claude-sonnet-4-6`. Then smoke-test: budget page → "Extract from file" → upload a Thai receipt (single) and a contractor scope-of-work (multiple) → review table → save all. (7) **apply M7 migration `db/migrations/0006_tasks_milestones.sql` to the Neon `production` branch** then restart dev with a clean `.next`, and smoke-test M7 (demo in `IMPLEMENTATION_PLAN.md` §10.3): on a flip add tasks with due dates (today / +3d / yesterday→overdue), assign some to self, check `/my-tasks` shows only yours, complete one (vanishes from open list), confirm overdue task shows destructive red; add timeline milestones, mark one hit, see on-time/late variance pill.
- **M7 deferred items:** automated tests (unit `dueDateBucket`, integration, Playwright) — same defer pattern as prior milestones (no test infra wired in repo); `relatedAssignmentId` / `flipStageId` task links exist in schema + actions but aren't pickable in `TaskForm` yet (trivial follow-up); role permissions still org-member-open (post-M7 pass — see `project_role_permissions_gap.md`); LINE/email due reminders (M11); task comments + attachments (M11 polymorphic comments/documents).
- **M6.7 deferred items:** subscription OAuth tokens are short-lived and NOT auto-refreshed from the env var (re-set the value when it expires — fine for an API key); the uploaded document is stored under `receipts/{org}/...` but not yet linked to the rows it produced (no receipt attached to the created transactions/lines — follow-up); T&M extraction maps each row to rate×1-day (labor) or cost+0%-markup (material) — operator adjusts after; no multi-file batch, no per-row confidence, no auto-categorization (operator picks category/budget-line per row); integration + Playwright tests (same defer pattern).

If a request is out of sequence with the current milestone, flag it.

## The rules that matter most

Full list: `CONVENTIONS.md` §17–18. The critical ones to hold firm on:

- **Multi-tenant safety:** every table has `organization_id`; every action starts with `requireAuth()` + `getActiveOrgId()`; every query filters by `organizationId` explicitly. **This app-level filter is the real tenant gate** — RLS is enabled on every table but has no policies (Prisma connects as table owner and bypasses it). Auth resolves via Neon Auth (`src/server/auth/`); `requireAuth`/`getActiveOrgId` signatures are unchanged from the Supabase era.
- **Migrations:** **Neon SQL is source of truth, not Prisma.** Hand-written SQL in `db/migrations/` applied via `psql "$DATABASE_URL_UNPOOLED"` or the Neon MCP — see `db/README.md`. Never `prisma migrate dev` / `db push`. Never edit an applied migration; write a new numbered file on top.
- **Type safety:** no `any`, no `!`, no `as X` without a comment. Server actions return `ActionResult<T>`, never throw to client.
- **Design:** monochrome base; color only on signals with good/bad directionality (budget variance, overdue, payment outcomes). Stages/roles/categories/priorities stay neutral. No Tailwind default palette. No arbitrary values.
- **i18n:** Thai authored first, English translated. Every string through `t()`. Buddhist Era dates in Thai locale.
- **No:** `localStorage`, `useEffect` for data fetching, global state libs, `console.log`, `process.env` outside `src/lib/env.ts`.

## The directionality test (most-applied rule)

Before applying color to any indicator: **does this state have an objectively better/worse version?**

- Yes → semantic variant (positive / warning / destructive) allowed
- No → neutral, always

"Sold" is a stage — neutral. "Paid" is an outcome — positive. "Approved" is mid-workflow — neutral. "Over budget 15%" is a state — destructive.

Full rules: `DESIGN_SYSTEM.md` §2.2.

## How to work with Ben

- He vibe-codes. He tells you what, you handle how — don't ask "hooks or class?"
- Match existing patterns over inventing better ones. Consistency > cleverness.
- Be direct. If a request will cause a problem, say so concretely + suggest the alternative + reference the doc.
- State decisions first, reasoning after. Don't write essays.
- Flag assumptions explicitly. Prefer making a call + noting it over asking five clarifying questions.
- When you deviate from a convention, say so and why.

## When in doubt

Consult the doc. If the doc doesn't cover it, ask Ben. Don't invent.

---

*Last updated: 2026-06-14 after M7 Tasks & Timeline complete. If rules here drift from `CONVENTIONS.md`, the canonical doc wins — update this file to match.*
